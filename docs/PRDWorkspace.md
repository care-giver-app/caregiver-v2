# Product Requirements

## About this document

This document serves as the workspace for ideas that I want to write down and eventually merge into the PRD. This document should not be deleted but if an idea is merged in to the PRD or scrapped entirely, then just that idea can be deleted from the work space.

## Workspace

Notes:
Create your first care team screen:
We should probably have the home screen instruct the user to create a receiver if they don't have an active receiver.
